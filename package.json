{
  "name": "@behavioml/explorer",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Initial scaffold for the BehavioML Model Explorer.",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "typecheck": "tsc -b --noEmit",
    "test": "node --test"
  },
  "dependencies": {
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "peerDependencies": {
    "@behavioml/validator": "^0.1.0"
  },
  "peerDependenciesMeta": {
    "@behavioml/validator": {
      "optional": true
    }
  },
  "devDependencies": {
    "@types/react": "^19.1.0",
    "@types/react-dom": "^19.1.0",
    "@vitejs/plugin-react": "^5.0.0",
    "typescript": "^5.8.0",
    "vite": "^7.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
