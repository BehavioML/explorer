description: |
  Retrieve a public unauthenticated `.tgz` or `.tar.gz` archive URL directly in the browser when possible.

requires:
  - archive/remote_archive_fetch

events:
  - workspace/archive_load_requested
  - workspace/archive_bytes_available
